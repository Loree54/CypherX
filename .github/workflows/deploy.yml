name: Auto & Manual Restart

on:
  workflow_dispatch:       # Allows manual trigger
  push:                    # Restarts on push
  schedule:
    - cron: "0 */6 * * *"  # Auto restart every 6 hours (UTC time)

jobs:
  restart:
    runs-on: ubuntu-latest
    steps:
      - name: Restart Service
        run: |
          echo "Restarting service..."
          # Put your restart commands here
          # Example: pm2 restart app_name
